Never add comments;
Always run unit tests and E2E2 tests and ensure everything is working before finishing your task;
All code written must have 100% unit test coverage;
All code written must have 100% integration test coverage;
E2E tests must fulfill all scenarios described in the .feature files;
If you have any questions about implementation, check the .feature files or the doc folder to understand the test scenarios and the expected behavior of the system;
You must update the documentation in the doc folder;
You must not change the architecture of existing projects and should ask if necessary;
You must not change the files CLAUDE.md;
The development environment is already running in watch mode on the front end (port: 5173) and back end (port: 3001), so changes will be reflected automatically. The database and Redis services are also already running, so there's no need to worry about that;